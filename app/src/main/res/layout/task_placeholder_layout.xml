<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    tools:viewBindingIgnore="true">

    <View
        android:id="@+id/vCheckBox"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:background="@color/label_placeholder"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/vDesc"
        android:layout_width="200dp"
        android:layout_height="16dp"
        android:layout_marginStart="12dp"
        android:background="@color/label_placeholder"
        app:layout_constraintStart_toEndOf="@+id/vCheckBox"
        app:layout_constraintTop_toTopOf="@+id/vCheckBox" />

    <View
        android:id="@+id/vDeadline"
        android:layout_width="60dp"
        android:layout_height="16dp"
        android:layout_marginTop="4dp"
        android:background="@color/label_placeholder"
        app:layout_constraintStart_toStartOf="@+id/vDesc"
        app:layout_constraintTop_toBottomOf="@+id/vDesc" />

    <View
        android:id="@+id/vInfo"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:background="@color/label_placeholder"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>